//Arrow Function
const verificarNumeroArrow = (x) => {
    if(x % 2 === 0){
        console.log(`${x} es par`);
    }else{
        console.log(`${x} es impar`);
    }
}

verificarNumeroArrow(8);
verificarNumeroArrow(15);